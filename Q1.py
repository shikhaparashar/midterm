ANSWER1:
METHOD OF STRINGS:
(1)Capitalise(a.capitalise()):It is used to convert the string into capital form.
(2)Title(a.title()):this function is used to convert the sting into title i.e only first letter of the title becomes capital.
(3)count(a.count()):this function is used to count the no. of the total characters in a strings.
(4)isdigit(a.isdigit()):this functions returns true value only if the string is consist of digits.

